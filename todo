[DOING] Create a list of mobs
[] Make skeletons shoot with the RandomArrow. Instructions:
    When triggered the entityJoinedWorld event, replace it by a custom
    skeleton with a config specified probability. keys:
        - AbstractSkeletonEntity: performRangedAttack
[] Make an else clause into the bow file in order to allow ai to shoot it
